{
  "schema_version": "0.1.0",
  "cases": [
    {
      "id": "required-reason-declared",
      "fixture": "fixtures/minimal-app",
      "invariants": [
        "PrivacyInfo.xcprivacy parses",
        "UserDefaults signal is covered",
        "no missing-reason finding"
      ]
    },
    {
      "id": "required-reason-gap",
      "fixture": "fixtures/privacy-gap",
      "invariants": [
        "missing manifest is not reported as confirmed noncompliance",
        "UserDefaults signal produces INFERRED NEEDS_VERIFY",
        "comments do not create duplicate evidence"
      ]
    },
    {
      "id": "dry-run-isolation",
      "fixture": "generated temporary Xcode project",
      "invariants": [
        "execution is opt-in",
        "output inside repository is refused",
        "run script phases require acknowledgement"
      ]
    }
  ]
}
