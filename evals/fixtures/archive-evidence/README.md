# Sanitized Archive evidence fixtures

These files contain synthetic bundle identifiers and synthetic team placeholders only. They exercise offline entitlement/profile wildcard comparison and the Privacy Manifest aggregate schema. The versioned Privacy Report schema is generated directly by the Archive test so its bundle inventory stays aligned with that test's synthetic Archive. None of this evidence is a real provisioning profile, signature, certificate, Apple report, or proof that any artifact is signed.
